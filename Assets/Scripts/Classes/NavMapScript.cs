using UnityEngine;
using System.Collections;

public class NavMapScript : MonoBehaviour
{

    private NavMesh navplace;
    // Use this for initialization
    void Start()
    {
        navplace = new NavMesh();
    }

    // Update is called once per frame
    void Update()
    {

    }
}
